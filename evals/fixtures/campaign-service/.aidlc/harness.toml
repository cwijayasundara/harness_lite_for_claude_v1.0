[project]
name = "campaign-service"
[capabilities]
test = "node --test tests/*.test.mjs"
[formats]
test = "tap"
[stages]
fast = ["secrets"]
stop = ["fast", "test"]
commit = ["stop", "scope-drift", "budget", "tamper"]
